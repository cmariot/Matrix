template <typename U>
U lerp(const U &u, const U &v, const double &t)
{
    /*
    Linear interpolation
    */

    U result(u);

    std::cout << result;

    (void)v;
    (void)t;

    return result;
}